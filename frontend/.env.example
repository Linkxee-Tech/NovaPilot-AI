# Frontend Environment
# Optional absolute API base URL (must include /api/v1).
# Leave blank to use same-origin '/api/v1' (recommended with nginx or Vite proxy).
VITE_API_URL=

# Dev-only proxy target used by Vite when VITE_API_URL is blank.
VITE_DEV_PROXY_TARGET=http://127.0.0.1:8000
